# 服务层

`services` 包含确定性、可复用的应用策略，这些策略不应归属于适配器或插件。

目前 `safety.py` 拥有 `SafetyPolicy`。它在运行时调度任何动作之前验证设备互锁、夹爪参数限位、感知数据的有效性和时效性、置信度、标定导出的坐标系以及支持的机器人指令。

请勿将安全决策移入规划器插件或适配器实现中。仅当多个运行时路径需要相同的确定性决策时，才在此处添加新的共享策略。
