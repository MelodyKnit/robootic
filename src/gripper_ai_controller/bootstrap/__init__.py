"""Runtime composition code that turns structured configuration files into components."""

from gripper_ai_controller.bootstrap.runtime_builder import build_runtime, load_runtime_config

__all__ = ["build_runtime", "load_runtime_config"]
