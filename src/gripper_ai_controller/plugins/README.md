# 插件

插件通过受约束的生命周期钩子和类型化事件来扩展行为。不允许导入适配器实例或调度硬件指令。

## 插件类型

- 感知插件：将 `ImageFrame` 数据转换为物体、姿态和抓取候选。
- 规划器插件：将有效的 `PerceptionResult` 转换为规范化指令提案。
- 观察者插件：记录或报告类型化运行时事件。

每个插件声明一个 `ComponentManifest`，并通过 Python 配置模块显式启用。生产更新需要重启。开发重载使用核心运行时生命周期，且不能在指令调度期间发生。
