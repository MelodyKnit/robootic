# 受版本控制的项目数据

此目录存放构建、测试或演示本项目所需的小型、非敏感且可复现的资源，例如仿真场景元数据、示例物体标签、标定模板和确定性测试夹具。

不得在此存放生产采集图像、私有标定结果、模型权重、密钥、可变状态或大型生成产物。这些内容应存放在 `localstore/`，且不纳入版本控制。

`vision-fixtures/` 是人体姿态离线验收的已授权公开图片集。具体来源、许可、哈希、场景和执行方式见 [视觉离线评测素材说明](vision-fixtures/README.md)。
